{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build-A-Bot Web App - Launch and Debug Standalone",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/Web/Server"
        }
    ]
}
